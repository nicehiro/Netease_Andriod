<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.nicehiro.fragmenttest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MallActivity"></activity>
        <activity android:name=".MallActivityWithArg"></activity>
        <activity android:name=".MallActivityWithTrans"></activity>
        <activity android:name=".MallActivityWithState"></activity>
        <activity android:name=".MallActivityWithComm"></activity>
    </application>

</manifest>